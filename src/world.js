/**
 * Greats the world the way it should be...
 * @returns {string} hello? ні
 */
function hello() {
    const payload = "Hello, cruel World...";
    console.log(payload);
    return payload;
}

hello();

export { hello };
