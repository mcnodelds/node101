/**
 * concats strings of an array
 * @param {Array<string>} strings
 * @returns {string} concatenated string
 */
const concat = (strings) => strings.join(" ");

console.log(concat(["Hello,", "cruel", "World("]));
